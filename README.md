# Music store
C#/.NET Core

##############################################################################################

App creates tables with records and injects them to database.
It allows to perform CRUD operations.
